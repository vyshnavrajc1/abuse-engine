openapi: 3.0.0
info:
  title: Sample API
  version: 1.0.0
paths:
  /api/users/{id}:
    get:
      operationId: getSingleUser
      parameters:
        - name: id
          in: path
          required: true
          x-owner: true
          schema:
            type: string
      responses:
        '200':
          description: OK